apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: hello-
  namespace: argo
spec:
  serviceAccountName: argo-workflow
  entrypoint: hello
  templates:
  - name: hello
    container:
      image: busybox
      command: [echo]
      args: ["hello argo workflows"]
